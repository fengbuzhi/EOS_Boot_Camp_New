#README

#Use a few accounts
#Using git pod, use chmod u+x to change the permission and add execution right.
chmod u+x compile.sh
chmod u+x bootstrap.sh
chmod u+x demo.sh

#Use a lot of accounts
#Using git pod, use chmod u+x to change the permission and add execution right.
chmod u+x compile.sh
chmod u+x bootstrap_new.sh
chmod u+x demo_new.sh

Just ignore all warnings as an user may have an account, or the files have been compiled, or an action is not allowed, etc.
